/*
 * lifecycle_state_mapper.cpp
 *
 * Lifecycle state mapping implementation (currently header-only,
 * this file is reserved for future extensions).
 */
#include "lifecycle_state_mapper.h"

namespace oh_adapter {

// Mapping logic is currently implemented as inline functions in the header.
// Extend here if more complex mapping logic is needed (e.g., state machines,
// event sequencing).

}  // namespace oh_adapter
